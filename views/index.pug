extends layout

block content
  .col-md-10.offset-md-1
    .card
      .card-header
        h1.card-title Member area
        .card-subtitle Welcome to the member area, #{user.name}!
      .card-body

        if users
          ol
            each user, i in users
              li #{user.name}
